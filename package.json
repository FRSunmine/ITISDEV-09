{
  "name": "sidequest-prototype",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node scripts/dev.js",
    "dev:frontend": "vite",
    "dev:api": "node --watch server/index.js",
    "build": "vite build",
    "db:reset": "node scripts/reset-db.js",
    "preview": "vite preview",
    "start": "node scripts/start.js",
    "test": "node --test"
  },
  "devDependencies": {
    "vite": "^7.0.0"
  },
  "dependencies": {
    "express": "^5.2.1"
  }
}
